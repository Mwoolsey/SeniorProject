<%= form_for(sms_alert) do |f| %>
  <% if sms_alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sms_alert.errors.count, "error") %> prohibited this sms_alert from being saved:</h2>

      <ul>
      <% sms_alert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :alertType %>
    <%= f.number_field :alertType %>
  </div>

  <div class="field">
    <%= f.label :frequency %>
    <%= f.number_field :frequency %>
  </div>

  <div class="field">
    <%= f.label :account_id %>
    <%= f.text_field :account_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
